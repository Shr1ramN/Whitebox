{
  "name": "whitebox",
  "displayName": "Whitebox",
  "description": "Python code analyzer extension",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:whitebox.analyzePythonFiles"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "whitebox.analyzePythonFiles",
        "title": "Analyze Python Files"
      }
    ]
  },
  "dependencies": {
    "node-fetch": "^3.1.0"
  }
}
